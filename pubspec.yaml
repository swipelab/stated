name: stated
description: final state
version: 0.0.7
homepage: https://swipelab.co/
repository: https://github.com/swipelab/stated

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
