name: stated
description: A neat set of tools aimed at simplyfind the state management. Stated<T>, Store, ObservableList,


version: 1.2.0
homepage: https://swipelab.co/
repository: https://github.com/swipelab/stated

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
